apiVersion: v1
kind: Service
metadata:
  name: {{ include "easyjs.fullname" . }}
  labels:
    {{- include "easyjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "easyjs.selectorLabels" . | nindent 4 }}
